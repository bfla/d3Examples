template(name="header")
  nav.navbar.navbar-default(role="navigation")
    div.container-fluid
      div.navbar-header
        button.navbar-toggle.collapsed(type="button" data-toggle="collapse" data-target="#navigation")
          span.sr-only Toggle navigation
          span.icon-bar 
          span.icon-bar
          span.icon-bar 
        a.navbar-brand(href="{{pathFor 'home'}}") d3ify
      div.collapse.navbar-collapse
        ul.nav.navbar-nav
          li
            a(href="{{pathFor 'barChart'}}") Bar
          li 
            a(href="{{pathFor 'fancyBar'}}") Fancy Bar
          li
            a(href="{{pathFor 'dimpleBar'}}") Dimple Bar
          li
            a(href="{{pathFor 'c3Bar'}} ") C3 Bar
          li
            a(href="{{pathFor 'nvd3Bar'}}") NVD3 Bar
          li
            a(href="{{pathFor 'misoBar'}}") Miso Bar
          li
            a(href="{{pathFor 'udacityTest'}}") Udacity Test
      //- div#navigation.collapse.navbar-collapse
      //-   ul.nav.navbar-nav 
      //-     //- li.dropdown
      //-       //- +notifications
      //-   ul.nav.navbar-nav.navbar-right
      //-     //- +loginButtons